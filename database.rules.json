
{
  "rules": {
    ".read": true,
    ".write": true,
    "users": {
      ".indexOn": ["email", "role"]
    },
    "shipments": {
      ".indexOn": ["clientId", "driverId", "status"]
    },
    "ratings": {
      ".indexOn": ["shipmentId", "fromUserId", "toUserId"]
    }
  }
}
