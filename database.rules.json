
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.role === 'admin')",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "shipments": {
      "$shipmentId": {
        ".read": "auth != null",
        ".write": false
      }
    },
    "ratings": {
      "$ratingId": {
        ".read": "auth != null",
        ".write": false
      }
    }
  }
}